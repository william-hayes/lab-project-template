## Raw data
**Data files generated by the experiment software.**

*Example:* A PsychoPy experiment produces separate CSV files for each subject.

## Processed data
**Data files that result from pre-processing the raw data.**

*Example:* The data files for each subject are aggregated into a master data file with renamed columns.

